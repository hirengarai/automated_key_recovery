
*.c
*.lp
*.pdf
*.pyc
files/*
files/autoguess/
# ...but the solver encodings are inputs, not output: they take minutes to
# regenerate and the result depends on which logic-minimisation backend is
# installed, so a fresh clone would otherwise reach a different (equally
# optimal) trail. OCP pins matrix_modeling for the same reason.
!files/matrix_modeling/
!files/sbox_modeling/
test/autoguess/files/
temp/
.pytest_cache/
**/temp/
.DS_Store
env312/
venv/
.venv/
env/

# machine-local editor/assistant settings
.claude/settings.local.json
__pycache__/

# carried over from the previous key-recovery repository
*.py[cod]
results/
constraints/
differential_trails/
test/**/files/output_*
test/**/*.gv
CLAUDE.md
